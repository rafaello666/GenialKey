{
    "compilerOptions": {
      // Kompatybilny target JS (możesz użyć np. ES2020, ESNext w zależności od potrzeb)
      "target": "ES2020", 
      // Sposób ładowania modułów - CommonJS dla Node, 
      // ale tutaj trzymamy ogólną konfigurację. 
      // Dla frontendu (Next) i tak nadpisujemy w "extends".
      "module": "commonjs",
  
      // Biblioteki wbudowane
      "lib": ["ES2020", "DOM"],
  
      // Wymusza ścisły tryb typowania
      "strict": true,
  
      // Ułatwia importowanie plików ES
      "esModuleInterop": true,
  
      // Pomija sprawdzanie zgodności typów w plikach definicji *.d.ts
      "skipLibCheck": true,
  
      // Chroni przed mieszaniem się wielkości liter w ścieżkach
      "forceConsistentCasingInFileNames": true
    },
    // Wykluczamy node_modules i ewentualne foldery build
    "exclude": ["node_modules", "**/dist", "**/.next"]
  }
  